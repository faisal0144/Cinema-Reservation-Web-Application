<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinema Authentication</title>
    <!-- Link to the external CSS file -->
    <link rel="stylesheet" href="styles.css">
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen">

    <!-- Main container for the auth page -->
    <div class="card">

        <!-- Logo/Title section -->
        <div class="card-header">
            <h1 class="card-title">Join the fun!</h1>
            <p class="card-subtitle">Create an account to get started.</p>
        </div>
            
        <!-- Warning Message Area (initially hidden) -->
        <div class="warning-message hidden" id="warning">
            An error occurred. Please try again.
        </div>
        
        <!-- Signup Form -->
        <form id="signup-form" action="/signup" method="post" class="form">
            <div class="form-group">
                <label for="signup-email" class="form-label">Email Address</label>
                <div class="mt-1">
                    <input id="signup-email" name="email" type="email" autocomplete="email" required
                        class="form-input">
                </div>
            </div>

            <div class="form-group">
                <label for="signup-password" class="form-label">Password</label>
                <div class="mt-1">
                    <input id="signup-password" name="password" type="password" autocomplete="new-password" required
                        class="form-input">
                </div>
            </div>
            
            <div class="form-group">
                <label for="confirm-password" class="form-label">Confirm Password</label>
                <div class="mt-1">
                    <input id="confirm-password" name="confirm-password" type="password" autocomplete="new-password" required
                        class="form-input">
                </div>
            </div>

            <div>
                <button type="submit" class="form-button">
                    Sign Up
                </button>
            </div>
        </form>

        <!-- Link to a separate signup page if needed -->
        <div class="card-footer">
            Already have an account? <a href="/login" class="form-link">Sign In</a>
            <br>
            <br>
            Are you an administrator? <a href="/admin-signup" class="form-link">Sign Up Here</a>
        </div>

    </div>
</body>
</html>
