<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Showtime</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex h-screen bg-gray-100">

  <!-- Sidebar -->
  <aside class="w-64 bg-gray-900 text-white flex flex-col">
    <div class="px-6 py-4 text-xl font-bold border-b border-gray-700">
      🎬 Cinema Admin
    </div>
    <nav class="flex-1 px-4 py-6 space-y-2">
      <a href="/admin/addmovie" class="block py-2 px-3 rounded hover:bg-gray-800">Add Movie</a>
      <a href="/admin/addshowTime" class="block py-2 px-3 rounded bg-gray-800">Add Showtime</a>
      <a href="/admin/bookings" class="block py-2 px-3 rounded hover:bg-gray-800">View Bookings</a>
    </nav>
    <div class="px-6 py-4 border-t border-gray-700">
      <a href="/" class="text-sm text-gray-400 hover:text-white">← Back to Site</a>
    </div>
  </aside>

  <!-- Main Content -->
  <div class="flex-1 flex flex-col">

    <!-- Top Bar -->
    <header class="bg-white shadow px-6 py-4 flex justify-between items-center">
      <h1 class="text-xl font-semibold">Add Showtime</h1>
      <button class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</button>
    </header>

    <!-- Content Area -->
    <main class="flex-1 p-6 overflow-y-auto">

      <!-- Add Showtime Form -->
      <div class="bg-white p-6 rounded-xl shadow max-w-2xl">
        <h2 class="text-lg font-semibold mb-4">Add a New Showtime</h2>

        {{#if message}}
          <div class="mb-4 p-3 bg-green-100 text-green-700 rounded">{{message}}</div>
        {{/if}}

        <form action="/admin/addshowTime" method="POST" class="space-y-4">
          <div>
            <label class="block text-sm font-medium">Movie</label>
            <select name="movie" class="w-full border rounded px-3 py-2" required>
              {{#each movies}}
                <option value="{{this._id}}">{{this.title}}</option>
              {{/each}}
            </select>
          </div>
          <div>
            <label class="block text-sm font-medium">Date</label>
            <input type="date" name="date" class="w-full border rounded px-3 py-2" required>
          </div>
          <div>
            <label class="block text-sm font-medium">Time</label>
            <input type="time" name="time" class="w-full border rounded px-3 py-2" required>
          </div>
          <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
            Add Showtime
          </button>
        </form>
      </div>

    </main>
  </div>

</body>
</html>
